
<%= form_for(@list) do |f| %>
  <% if @list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
        <% list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="card">
    <div class="card-body">
        <div class="field form-group">        
            <% f.label :description %> <br/>
            <%= f.text_field :description %>
        </div>

        <div class="actions form-group">
            <%= f.submit "Add To do Activity", class: "btn btn-info" %>
        </div>
    <% end %>

    <br>
    <%= link_to 'Delete Marked Items', home_trashall_path, data: { confirm: 'Are you sure?'}, class: "btn btn-success"  %>
    <%= link_to 'Delete All your daily activities', home_trashthemall_path, data: { confirm: 'Are you sure?'}, class: "btn btn-success"  %>

 </div>
</div>